framework:
    messenger:
        transports:
            ranking.all: "%env(RANKING_ALL_EVENT)%"

        routing:
#            'App\DataTransferObject\RankingAllEvent':
#                - ranking.all
